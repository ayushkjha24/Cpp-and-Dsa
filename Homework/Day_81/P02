int power(int base, int exponent) {
if (exponent == 0)
return 1;
return base * power(base, exponent - 1);
}

Recursive calls : power(base, exponent ) -> power(base,
exponent -1 )-> … …… … ->power(base, 0 ).
So, no of recursive calls are dependent on exponent variable.

Time Complexity : O(exponent)
Space Complexity : O(exponent)